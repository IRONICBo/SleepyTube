# 语速调整 UX 测试指南

## 🎯 测试目标

验证以下功能正常工作：
1. ✅ 浮动状态面板显示
2. ✅ 通知弹窗显示
3. ✅ 速度变化反映在UI上
4. ✅ 用户手动调速后暂停30秒
5. ✅ 暂停/恢复按钮工作
6. ✅ 关闭按钮工作

## 📋 测试前准备

### 1. 重新加载扩展

```bash
# 方法1：在 Chrome 扩展页面重新加载
1. 打开 chrome://extensions/
2. 找到 "SleepyTube"
3. 点击刷新图标 🔄

# 方法2：完全重新安装
1. chrome://extensions/
2. 移除 SleepyTube
3. 点击 "加载已解压的扩展程序"
4. 选择 extension 文件夹
```

### 2. 打开开发者工具

在 YouTube 页面：
```
Windows/Linux: F12 或 Ctrl+Shift+I
Mac: Cmd+Option+I
```

### 3. 选择测试视频

推荐使用语速较快的视频，例如：
- 新闻播报（快速）
- 脱口秀（中等）
- ASMR耳语（慢速）

## 🧪 测试步骤

### 测试 1: 启用语速调整并查看浮动面板

**步骤**：
1. 打开 YouTube 视频
2. 右键点击 SleepyTube 按钮
3. 在弹出的设置面板中：
   - 找到 "Speech Rate" 部分
   - 点击 "Enable" 按钮
   - 选择目标速度（例如 "Normal"）

**预期结果**：

✅ **通知弹窗**（右上角，3秒后消失）：
```
┌────────────────────────────┐
│ ✅ Speech Rate Active      │
│ Target: Normal             │
└────────────────────────────┘
```

✅ **浮动状态面板**（右上角，距顶部80px）：
```
┌─────────────────────────────┐
│ 🎙️ Speech Rate      − × │
├─────────────────────────────┤
│ Detected:  —                │
│ Speed:     1.00x            │
│ Target:    Normal           │
│ Status:    Active           │
├─────────────────────────────┤
│     ⏸ Pause                │
└─────────────────────────────┘
```

**如何确认**：
- 打开开发者工具 (F12)
- 切换到 "Elements" 标签
- 搜索 `st-rate-indicator`（Ctrl+F 输入）
- 应该能看到整个面板的 HTML 结构

**Console 日志**：
```
SleepyTube: Speech rate adjustment enabled: normal
```

---

### 测试 2: 验证语速检测

**步骤**：
1. 播放视频
2. 等待 3-5 秒让检测启动
3. 观察面板变化

**预期结果**：

✅ **面板实时更新**（每2秒）：
```
┌─────────────────────────────┐
│ 🎙️ Speech Rate      − × │
├─────────────────────────────┤
│ Detected:  5.2 syl/s        │  ← 实时更新
│           (very_fast)       │  ← 类别显示
│ Speed:     0.75x            │  ← 橙色（减速）
│ Target:    Normal           │
│ Status:    Active           │  ← 绿色
├─────────────────────────────┤
│     ⏸ Pause                │
└─────────────────────────────┘
```

**速度颜色编码**：
- 🟠 **橙色背景** (`0.75x`): 速度 < 0.9x（减速）
- 🟢 **绿色背景** (`1.00x`): 速度 0.9x-1.1x（正常）
- 🔵 **蓝色背景** (`1.25x`): 速度 > 1.1x（加速）

**如何确认**：
```javascript
// 在 Console 中查看实时状态
window.SleepyTubeController.audioEngine.speechRateController.getStatus()

// 输出示例：
{
  enabled: true,
  detected: {
    syllablesPerSecond: 5.2,
    category: "very_fast",
    confidence: 0.85
  },
  adjustment: 0.75,
aybackRate: 0.75,
  isPaused: false,
  userManuallySet: false
}
```

**Console 日志**（每2秒）：
```
SleepyTube: Speech rate update: {
  detected: "5.20 syl/s",
  category: "very_fast",
  confidence: "85%",
  playbackRate: "0.75x"
}
```

---

### 测试 3: 速度显著变化时的通知

**步骤**：
1. 播放视频一段时间（当前速度稳定）
2. 等待检测到显著变化（> 10% 差异）

**预期结果**：

✅ **蓝色通知弹窗**：
```
┌────────────────────────────┐
│ ⚡ Speed Adjusting         │  ← 蓝色左边框
│ 1.00x → 0.75x              │
└────────────────────────────┘
```

**触发条件**：
- 新调整值与当前值差异 > 0.1（10%）
- 例如：从 1.0x 调整到 0.75x

**动画**：
- 从右侧滑入（0.3秒）
- 显示 3 秒
- 淡出消失（0.3秒）

**如何强制触发**：
```javascript
// 在 Console 中手动触发
const ctrl = window.SleepyTubeController.audioEngine.speechRateController;
ctrl.currentAdjustment = 1.0;
ctrl.updatePlaybackRate(); // 会触发通知
```

---

### 测试 4: 用户手动调速 + 30秒暂停

**步骤**：
1. 等待自动调速稳定（例如 0.75x）
2. 点击 YouTube 播放器右下角的设置按钮
3. 选择 "播放速度" → 设置为 "1"

**预期结果**：

✅ **橙色警告通知**：
```
┌────────────────────────────┐
│ ⚠️ Manual Speed            │  ← 橙色左边框
│ Auto-adjustment paused for │
│ 30s                        │
└────────────────────────────┘
```

✅ **面板状态更新**：
```
┌─────────────────────────────┐
│ 🎙️ Speech Rate      − × │
├─────────────────────────────┤
│ Detected:  5.2 syl/s        │
│ Speed:     1.00x            │  ← 用户设置的速度
│ Target:    Normal           │
│ Status:    Paused       │  ← 橙色，显示暂停
├─────────────────────────────┤
│     ⏸ Pause                │
└─────────────────────────────┘
```

✅ **Console 日志**：
```
SleepyTube: User manually set speed to 1
```

**时间线验证**：
```
T=0s:   用户手动设置 1.0x
T=0-30s: 速度保持 1.0x（暂停自动调整）
        Status 显示 "Paused"（橙色）
T=30s:  恢复自动调整
        Status 变回 "Active"（绿色）
        速度可能重新调整
```

**如何确认 30 秒计时**：
```javascript
// 在 Console 中检查
const ctrl = window.SleepyTubeController.audioEngine.speechRateController;
console.log('User manually set:', ctrl.userManuallySet);
console.log('Time since:', Date.now() - ctrl.lastUserSetTime, 'ms');
console.log('Grace period remaining:', Math.max(0, 30000 - (Date.now() - ctrl.lastUserSetTime)), 'ms');
```

---

### 测试 5: 暂停/恢复按钮

**步骤 A - 暂停**：
1. 点击面板中的 "⏸ Pause" 按钮

**预期结果**：

✅ **按钮变化**：
```
⏸ Pause  →  ▶ Resume
```

✅ **状态变化**：
```
Status: Active (绿色) → Paused (橙色)
```

✅ **速度冻结**：
- 当前播放速度不再改变
- 即使检测到新的语速也不调整

**步骤 B - 恢复**：
1. 点击 "▶ Resume" 按钮

**预期结果**：

✅ **按钮恢复**：
```
▶ Resume  →  ⏸ Pause
```

✅ **状态恢复**：
```
Status: Paused (橙色) → Active (绿色)
```

✅ **继续调整**：
- 恢复每 2 秒检测和调整速度

**验证逻辑**：
```javascript
// 检查暂停状态
const ctrl = window.SleepyTubeController.audioEngine.speechRateController;
console.log('Is paused:', ctrl.isPaused);

// 模拟点击
document.getElementById('st-rate-toggle').click();
```

---

### 测试 6: 最小化按钮

**步骤**：
1. 点击面板右上角的 "−" 按钮

**预期结果**：

✅ **面板折叠**：
```
Before:                      After:
┌───────────────────┐       ┌───────────────────┐
│ 🎙️ Speech Rate − ×│       │ 🎙️ Speech Rate − ×│
├───────────────────┤       └───────────────────┘
│ Detected: 5.2     │       (body 和 footer 隐藏)
│ Speed: 0.75x      │
│ Target: Normal    │
│ Status: Active    │
├───────────────────┤
│   ⏸ Pause        │
└───────────────────┘
```

✅ **再次点击 "−"**：
- 面板展开回原样

**CSS 验证**：
```css
/* 折叠后添加的 class */
.st-rate-panel.minimized .st-rate-body,
.st-rate-panel.minimized .st-rate-footer {
  display: none;
}
```

---

### 测试 7: 关闭按钮

**步骤**：
1. 点击面板右上角的 "×" 按钮

**预期结果**：

✅ **蓝色通知**：
```
┌────────────────────────────┐
│ ℹ️ Speech Rate Disabled    │  ← 蓝色左边框
│ Speed restored             │
└────────────────────────────┘
```

✅ **面板消失**：
- 整个 `#st-rate-indicator` 元素从 DOM 移除

✅ **速度恢复**：
- `video.playbackRate` 恢复到原始值（通常是 1.0）

✅ **功能禁用**：
- 不再检测语速
- 不再调整速度
- 定时器清除

✅ **Console 日志**：
```
SleepyTube: Speech rate adjustment disabled
```

**验证**：
```javascript
// 检查 DOM
document.getElementById('st-rate-indicator'); // 应该是 null

// 检查状态
const ctrl = window.SleepyTubeController.audioEngine.speechRateController;
console.log('Enabled:', ctrl.isEnabled); // false

// 检查视频速度
const video = document.querySelector('video');
console.log('Playback rate:', video.playbackRate); // 1.0
```

---

## 🎨 UI 元素位置确认

### 浮动面板位置
```css
position: fixed;
top: 80px;          /* 距顶部 80px */
right: 20px;        /* 距右侧 20px */
z-index: 9999;      /* 最高层级 */
```

**如何验证**：
1. F12 打开开发者工具
2. Elements 标签
3. 找到 `#st-rate-indicator`
4. 查看 Computed 样式

### 通知位置
```css
position: fixed;
top: 20px;          /* 距顶部 20px */
right: 20px;        /* 距右侧 20px */
z-index: 10000;     /* 比面板高 */
```

## 🐛 常见问题排查

### 问题 1: 面板不显示

**检查步骤**：
```javascript
// 1. 检查功能是否启用
const ctrl = window.SleepyTubeController.audioEngine.speechRateController;
console.log('Enabled:', ctrl.isEnabled);

// 2. 检查 DOM
console.log('Panel exists:', !!document.getElementById('st-rate-indicator'));

// 3. 检查样式是否加载
console.log('Styles loaded:', !!document.getElementById('st-rate-styles'));

// 4. 手动创建面板测试
if (ctrl.isEnabled && !ctrl.indicator) {
  ctrl.createIndicator();
}
```

### 问题 2: 数据不更新

**检查步骤**：
```javascript
// 1. 检查检测器状态
const detector = ctrl.detector;
console.log('Detector running:', detector.isRunning);

// 2. 检查更新定时器
console.log('Update interval:', ctrl.updateInterval);

// 3. 查看最新检测结果
console.log('Latest rate:', detector.getRate());

// 4. 手动触发更新
ctrl.updatePlaybackRate();
ctrl.updateIndicatorUI('active');
```

### 问题 3: 通知不显示

**检查步骤**：
```javascript
// 1. 手动显示通知测试
const ctrl = window.SleepyTubeController.audioEngine.speechRateController;
ctrl.showNotification('Test Title', 'Test Description', 'info');

// 2. 检查 DOM 中的通知
document.querySelectorAll('.st-notif');

// 3. 检查是否被其他元素遮挡
// 在 Elements 中查看 z-index
```

### 问题 4: 颜色编码不正确

**检查步骤**：
```javascript
// 查看当前速度和颜色
const speedEl = document.getElementById('st-speed');
console.log('Speed text:', speedEl.textContent);
console.log('Background:', speedEl.style.background);
console.log('Color:', speedEl.style.color);

// 预期：
// < 0.9x: rgba(255, 152, 0, 0.2), #FF9800
// 0.9-1.1x: rgba(76, 175, 80, 0.2), #4CAF50
// > 1.1x: rgba(33, 150, 243, 0.2), #2196F3
```

## ✅ 完整测试检查清单

### 基础功能
- [ ] 浮动面板显示在正确位置
- [ ] 启用时显示绿色通知
- [ ] 禁用时显示蓝色通知
- [ ] 面板显示所有 4 个字段（Detected, Speed, Target, Status）

### 实时更新
- [ ] Detected 每 2 秒更新
- [ ] Speed 显示当前播放速度
- [ ] 速度颜色编码正确（橙/绿/蓝）
- [ ] Status 反映当前状态（Active/Paused）

### 用户控制
- [ ] 暂停按钮切换为恢复按钮
- [ ] 恢复按钮切换回暂停按钮
- [ ] 最小化按钮折叠/展开面板
- [ ] 关闭按钮移除面板并恢复速度

### 智能行为
- [ ] 用户手动调速触发橙色警告
- [ ] 30 秒内不覆盖用户设置
- [ ] 30 秒后恢复自动调整
- [ ] 显著速度变化显示蓝色通知

### 动画效果
- [ ] 通知从右侧滑入
- [ ] 通知 3 秒后淡出
- [ ] 按钮 hover 效果正常
- [ ] 面板展开/折叠平滑

## 📹 推荐测试视频

### 快速语速（测试减速）
- 新闻播报
- 快节奏脱口秀
- 技术讲解视频

### 正常语速（测试稳定性）
- 播客节目
- 访谈节目
- 教育视频

### 慢速语速（测试加速）
- ASMR 耳语
- 冥想引导
- 缓慢朗读

## 🎯 成功标准

所有以下条件满足才算测试通过：

✅ **可见性**: 所有 UI 元素清晰可见，无遮挡
✅ **准确性**: 数据实时更新，数值正确
✅ **响应性**: 按钮点击立即响应
✅ **一致性**: 行为符合预期，无意外
✅ **性能**: 无明显卡顿，60 FPS 流畅

---

## 🚀 快速测试脚本

在 YouTube 页面的 Console 中运行：

```javascript
// === 快速测试脚本 ===
(function() {
  const ctrl = window.SleepyTubeController?.audioEngine?.speechRateController;
  
  if (!ctrl) {
    console.error('❌ SpeechRateController not found!');
    return;
  }
  
  console.log('=== Speech Rate Controller Test ===');
  console.log('✅ Controller found');
  console.log('Enabled:', ctrl.isEnabled);
  console.log('Paused:', ctrl.isPaused);
  console.log('User manual:', ctrl.userManuallySet);
  
  const panel = document.getElementById('st-rate-indicator');
  console.log('Panel exists:', !!panel);
  
  const styles = document.getElementById('st-rate-styles');
  console.log('Styles loaded:', !!styles);
  
  const status = ctrl.getStatus();
  console.log('\nCurrent Status:', status);
  
  console.log('\n=== Manual Tests ===');
  console.log('Show notification:', 'ctrl.showNotification("Test", "Works!", "info")');
  console.log('Toggle pause:', 'document.getElementById("st-rate-toggle").click()');
  console.log('Force update:', 'ctrl.updatePlaybackRate()');
  console.log('Check panel:', 'document.getElementById("st-rate-indicator")');
  
  console.log('\n=== All checks passed! ===');
})();
```

复制粘贴到 Console，按回车即可运行！
